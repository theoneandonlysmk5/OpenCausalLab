# Pinned dependencies for OpenCausalLab (verified environment, 2026-08).
# Install: python -m pip install -r requirements.txt

# Core replication (ETL + Tables 1–6)
pandas==2.3.3
numpy==2.2.6
scipy==1.15.3
statsmodels==0.14.6
pyarrow==25.0.0
pyreadstat==1.3.5

# Tests
pytest==9.1.1
pytest-cov>=5.0

# Causal ML extension (scripts/run_causal_ml.py)
scikit-learn==1.7.2
econml==0.17.0

# Dev / formatting (optional)
ruff>=0.8
black>=24.0
