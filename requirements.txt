# Dependency source of truth: pyproject.toml
# ([project.dependencies] + [project.optional-dependencies]).
#
# Prefer installing from package metadata:
#   python -m pip install -e ".[replication,dev]"
#   python -m pip install -e ".[all]"
#
# This file exists only for tools that require a requirements path.
# It does not maintain a second pin set — it installs the package extras.
-e ".[all]"
